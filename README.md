ArchaeoToolbox: software for geometric morphometrics in the 3D domain. Add commentMore actions




To use, you can compile the source files or download the installer (for Windows users) from the Binaries folder. An appimage has been added for Linux users, which doesn't need installation (from properties, Permissions, make it executable).


You can compile the source files or download the installer (for Windows users) from the Binaries folder. If you decide to compile it yourself, remember to modify the cmake file! 

