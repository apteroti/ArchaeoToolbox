ArchaeoToolbox: software for geometric morphometrics in the 3D domain.



To use, you can compile the source files (recommended for programmers) or download the installer (for Windows users) from the Binaries folder. An appimage also has been added for Linux users, which doesn't need installation (from properties, Permissions, make it executable).

If you decide to compile it yourself, remember to modify the cmake file! 

